# Copy to .env and fill in to enable live LLM answers.
# If unset, Sentinel-Med falls back to deterministic response templates
# so the demo always runs offline.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
